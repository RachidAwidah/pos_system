# مرجع مشروع POS

## تجهيز النشر وGitHub (2026-09-17)

- أصبح جذر المشروع مستودع Git موحدًا على فرع `main`، ويحتوي `backend/` و`frontend/` مع `docker-compose.yml` الجذري. نُقلت بيانات Git المتداخلة السابقة إلى `.git-snapshots/` محليًا وهي مستثناة من الرفع، للحفاظ على إمكانية الرجوع دون إنشاء مستودعين متداخلين.
- أُضيفت حماية سياق Docker من `.env` والاعتماديات، ودعم `pdo_mysql` لصورة الباك. أُضيف Docker/Nginx للفرونت مع SPA fallback وhealthcheck.
- أصبحت أصول CORS قابلة للضبط عبر `CORS_ALLOWED_ORIGINS`، وأزيل مسار شهادة Stripe الشخصي الثابت؛ أُكملت متغيرات Stripe وFXFeed في `.env.example`، وأزيل مفتاح Stripe المنشور الثابت من الفرونت لصالح إعداد runtime.
- أزيلت Workflows النشر القديمة الموروثة من ngx-admin، وأضيف CI مستقل للباك والفرونت، مع تحديث تعليمات النشر في README للباك والفرونت.
- وفق تقرير OpenCode المرفق: نجحت 158/158 من اختبارات Laravel على قاعدة بيانات مزروعة حديثًا، لكنها بقيت 150 اختبارات مصنفة Risky والسبب الدقيق غير محسوم. تعديل `phpunit.xml` الخاص بتخفيف strictness موجود ضمن الحالة الحالية. لم يُعَد تشغيل كامل الاختبارات في مرحلة توحيد Git.
- تحقق الرفع المستهدف: 1047 ملفًا مرحليًا، لا ملفات أكبر من 10MB، ولا أنماط مفاتيح GitHub/AWS/Stripe أو مفاتيح خاصة. ملفات `.env` الحقيقية و`vendor` و`node_modules` و`dist` وقاعدة SQLite والسجلات مستثناة؛ `backend/.env.example` مرفوع كقالب فقط.
- الخطوة التالية: إنشاء commit للمستودع الموحد، دفع `main` إلى `Rasheedowida/pos_system` والتحقق من وجود المجلدين عن بُعد، ثم حذف مستودع `Rasheedowida/pos_system_frontend` المنفصل بعد نجاح التحقق.

## تخفيف التحقق من مبالغ المرتجعات واسترداد الرصيد (2026-09-14)

- **السبب**: التحقق في `SalesReturnService::complete()` فرض تطابقًا دقيقًا بين مجموع مدفوعات الرد والمبلغ النقدي للمرتجع (`bccomp !== 0`)، مما يرفض المرتجعات الجزئية حتى لو كانت المبالغ صحيحة. كما افتقر الواجهة أمامية لحساب تلقائي لمبلغ الرد الأقصى.
- **الباك إند** (`SalesReturnService.php`):
  - استُبدلت فحص التطابق الدقيق بفحص `bccomp($provided, $returnTotal) === 1` (يُرفض فقط إذا تجاوز الرد إجمالي المرتجع).
  - أُضيف حساب تلقائي للرصيد: إذا قدم المستخدم مبلغًا نقديًا أقل من النسبة المدفوعة، يُضاف الفرق إلى رصيد المتجر للعميل.
  - أُضيف حماية: لا يجوز أن يتجاوز الرصيد المكتسب (due_amount) الرصيد المتبقي للمورد.
  - رسالة الخطأ: "Refund payments cannot exceed the return total." و"Refund payments must be at least {min} to stay within the outstanding balance."
- **الفرونت** (`pos-order-history.component.ts`):
  - أُضيف `get totalReturnValue()` و `get maxCashRefund()` و `get minCashRefund()` و `get creditAmount()` لحساب القيم ديناميكيًا بناءً على الأصناف المختارة.
  - `maxCashRefund` يُعيد قيمة المرتجع الكاملة (أقصى رد نقدي)، و`minCashRefund` يُعيد الحد الأدنى النقدي (القيمة ناقص due_amount).
  - `totalReturnValue` يستخدم `unit_price * returnQty` كبديل آمن لتجنب مشاكل القسمة.
  - `onReturnQuantityChange()` يملأ مبلغ الرد تلقائيًا بأقصى قيمة نقدي (`maxCashRefund`).
  - أُضيف عرض ملخص المرتجع: قيمة المرتجع، أقصى رد نقدي، وملاحظة الرصيد إذا كان هناك فرق.
  - أُضيف `[max]="maxCashRefund"` لحقل مبلغ الرد.
- **الترجمات**: أُضيف `RETURN_VALUE`، `MAX_CASH_REFUND`، `CREDIT_NOTE` بالعربية والإنجليزية.
- **الاختبارات** (`SalesReturnServiceTest.php`):
  - `test_less_cash_refund_rebalances_to_store_credit`: مرتجع نقدي جزئي مع تحويل الفرق إلى رصيد المتجر.
  - `test_refund_exceeding_paid_portion_is_rejected`: رفض المرتجع الذي يتجاوز إجمالي المرتجع.
  - `test_refund_exceeding_per_method_limit_is_rejected`: رفض المرتجع الذي يتجاوز حد طريقة الدفع.
  - `test_full_cash_refund_on_partially_paid_order`: تحقق من صحة المرتجع النقدي الكامل على طلبية مدفوعة جزئيًا (due_amount=2.20، refunded_amount=5.00، paid_amount=2.80).
  - صُلحت اختبارات سابقة بإضافة `product->update(['price' => '1.00'])` لأن SKU-001 سعره $1.05.
- **التحقق**: نجح `ngc --noEmit` و9 اختبارات SalesReturn (32 تحققًا) وPint.
- **تحديث role.md**: 2026-09-14.

## إصلاح حشو المتصفح التلقائي في نموذج المستخدمين (2026-09-14)

- **السبب**: حقول الإدخال في نموذج إضافة مستخدم جديدة lacked خصائص `autocomplete` و `name` و `type` مميزة، فحشو المتصفح البريد الإلكتروني المحفوظ (admin@example.com) في حقل الهاتف وكلمة المرور في حقل كلمة المرور الاختيارية.
- **الحل**:
  - أُضيف `autocomplete="email"` و `name="user_email"` لحقل البريد.
  - أُضيف `type="tel"` و `autocomplete="tel"` و `name="user_phone"` لحقل الهاتف (كان ينقصه type).
  - أُضيف `autocomplete="new-password"` و `name="user_password"` / `name="user_password_confirmation"` لحقلَي كلمة المرور.
  - أُضيف `name="user_name"` و `name="user_role"` لبقية الحقول.
- **الملفات المعدلة**: `pos-users.component.ts` (القالب فقط).
- **التحقق**: نجح `ngc --noEmit` وSCSS بدون أخطاء. لا تغييرات في المنطق البرمجي أو الباك.
- **تحديث role.md**: 2026-09-14.

## تنسيق النوافذ وشارت المخزون (2026-09-14)

- وُحّدت أنماط النوافذ المشتركة والقوالب المحقونة ونافذة Stripe، مع حقول متناسقة وتمرير داخلي وتخطيط متجاوب واتجاهات عربية/إنجليزية.
- أضيفت ألوان متنوعة وفواصل بيضاء للشارت الدائري بالمخزون دون تعديل بياناته أو منطق الدفع.
- التحقق قيد التنفيذ: بناء Angular ومعاينة العرض حسب توفر المتصفح؛ لم تُجرَ أي دفعة. الخطوة التالية: استكمال الفحص وتوثيق النتائج والحدود.


## إصلاح كشف الشفت في تاريخ الطلبيات — shiftId فارغ (2026-09-14)

- **السبب**: المكون كان يقرأ `localStorage.getItem('shiftId')` — مفتاح لا يكتبه أحد. المكون يخزّن الشفت في `pos_current_shift` كـ JSON يحتوي `{id: '...', ...}`.
- **الحل**: أُنشئت `syncShiftId()` عامة تقرأ `pos_current_shift` وتستخرج `.id` من JSON. تُستدعى في المنشئ ودقيقة polling كل 1.5 ثانية. أُزيل `storedShiftId()` الخاصة القديمة. أُضيف زر "إعادة فحص الشفت" وسطر debug مؤقت.
- **الترجمات**: أُضيف `ORDERS.RECHECK_SHIFT` بالعربية والإنجليزية.
- **الملفات المعدلة**: `pos-order-history.component.ts`، `ar.json`، `en.json`.
- **التحقق**: نجح `ngc --noEmit`. لا تغييرات في الباك.
- **تحديث role.md**: 2026-09-14.

## إصلاح صفحات تاريخ الطلبيات فارغة بعد تعديل shift_id (2026-09-14)

- **السبب**: عند إضافة `ngOnInit` لـ `PosOrderHistoryComponent` (لفلترة الشفت وطرق الدفع)، نُسي استدعاء `this.loadOrders()`. لم يكن هناك استدعاء أولي لجلب الطلبيات.
- **الحل**: إضافة `this.loadOrders()` في بداية `ngOnInit()` في `pos-order-history.component.ts`.
- **الملفات المعدلة**: `frontend/src/app/pages/pos-order-history.component.ts` (سطر واحد).
- **التحقق**: نجح `ngc --noEmit`. API يُرجع 635 فاتورة. لا تغييرات في الباك أو قاعدة البيانات.
- **تحديث role.md**: 2026-09-14.

## جعل الوردية اختيارية للمرتجعات بدون رد نقدي (2026-09-14)

- **السبب**: المرتجعات التي لا تشمل ردًا نقديًا (إعادة للمخزون، تبديل، رصيد متجر) كانت تتطلب شفتًا مفتوحًا رغم عدم الحاجة إليه.
- **الباك إند**:
  - Migration `2026_09_14_000001_make_shift_id_nullable_in_sales_returns_and_payments.php`: جعل `sales_returns.shift_id` و `payments.shift_id` كلاهما nullable.
  - `CompleteSalesReturnRequest.php`: `shift_id` من `required` إلى `nullable`.
  - `SalesReturnController::store()`: شرط إيجاد الشفت `Shift::find()` بدلاً من `findOrFail()`.
  - `SalesReturnService::complete()`: التوقيع `?Shift $shift`؛ التحقق من الشفت يعتمد على نوع المرتجع: إذا كان هناك رد نقدي (refunds بـ amount > 0) والشفت null → خطأ DomainException. إذا كان الشفت موجودًا → يُتحقق من الحالة والملكية والمستودع كالمعتاد. إذا لم يكن هناك شفت ولا رد نقدي → المرتجع يُنفَّذ بدون شفت.
- **الفرونت**:
  - `pos-order-history.component.ts`: زر المرتجع `[disabled]="returning || !canSubmitReturn()"`؛ الدالة الجديدة `canSubmitReturn()` تتحقق من: عدم الإرجاع، سبب المرتجع، والشفت فقط عند وجود رد نقدي. `submitReturn()` يُرسل `shift_id` فقط إذا كان موجودًا.
  - مراقبة polling كل ثانيتين لـ `localStorage shiftId` مع `OnDestroy` لتنظيف الاشتراك.
  - تنبيه `.cash-refund-alert` بالعربي والإنجليزي عند عدم وجود شفت.
  - `OrderService.returnOrder()`: `shift_id` أصبح optional في الـ payload.
- **ترجمات جديدة**: `ORDERS.REFUND_CASH_NEEDS_SHIFT` و `ORDERS.OPEN_POS` بالعربية والإنجليزية.
- **التحقق**: نجح `ngc --noEmit` و `php artisan migrate` و `pint --dirty`. لم تُختبر الواجهة بصريًا في المتصفح.
- **تحديث role.md**: 2026-09-14.

## إصلاح دورة الشفت (2026-09-14، مكتمل برمجيًا)

- نجح أيضًا فحص جميع مفاتيح الترجمة المستخدمة في قالب المبيعات بالعربية والإنجليزية، مع كشف النصوص المتضررة من الترميز ضمن test-shifts.cjs.

- أُلغي الإغلاق التلقائي عند logout/logout-all: يبقى الشفت مفتوحًا حتى العد والإغلاق الصريح. هذا يلغي التوثيق القديم الذي اعتبر النقد المتوقع نقدًا معدودًا.
- تجاوز فرق 50 دولارًا يتطلب أدمن فعليًا وسببًا مسجلًا؛ الإغلاق الإداري يستخدم سببه نفسه للتجاوز. أضيف ملخص نقدي للمالك أو الأدمن عبر shifts/{shift}/summary. الفرونت يعرض الملخص بالدولار مع مبلغ معدود فارغ والفرق، ويربط زر الإغلاق الإداري بمساره الصحيح ويمنع متابعة شفت شخص آخر.
- تصفير الافتتاح عند تبديل الصندوق وتجاهل الاستجابات القديمة وعدم استبدال مبلغ يدوي، وتحديث قائمة الصناديق بعد الإغلاق. هدف الإغلاق الإداري ثابت حتى لو تغير الصندوق المختار؛ منع الطلبات المكررة والمبلغ الفارغ، وقبول صفر معدود صراحة. أضيف توضيح الدولار وبقاء الشفت مفتوحًا بعد الخروج.
- التحقق: نجحت CashSessionApiTest وCashSessionServiceTest وAuthTest (22 اختبارًا، 93 تحققًا)، باستخدام سجلات factories داخل DatabaseTransactions تُلغى بعد الاختبارات. تغطي حدود ±50، سبب الفرق والتجاوز، رفض الكاشير وقبول الأدمن وتسجيل السبب، صلاحية الملخص، وعدم تسوية النقد عند logout/logout-all. لم تُغلق أو تُعدّل شفتات المستخدم للاختبار.
- نجح scripts/test-shifts.cjs للخدمات والمكون الفعليين مع HTTP وهمي: الإغلاق العادي والإداري، إرسال السبب، المبلغ الفارغ والصفر، تكرار الطلب والفشل، ملكية الشفت واستجابات الصناديق المتأخرة. نجح test-logout.cjs وngc --noEmit وPint على الملفات المحددة وgit diff --check؛ توليد Swagger ومسار summary ناجحان. لم تُجرَ معاينة متصفح أو تجربة عدّ نقد حقيقية. التالي معاينة المستخدم لنموذج الإغلاق الجديد؛ لا اختبارات برمجية مطلوبة متروكة.

## تحديث اسم المتجر فور الحفظ (2026-09-13)

- رُبط اسم المتجر في الهيدر بنفس استجابة تحميل الإعدادات وإشعار الحفظ الناجح، وأزيل طلب الاسم المنفصل. يظهر الاسم الجديد فور نجاح الحفظ دون إعادة تحميل الصفحة؛ فشل الحفظ لا يرسل إشعارًا ولا يغير الاسم.
- التحقق: نجح ngc --noEmit واختبارا الهيدر الحاليان للعملة وتسجيل الخروج؛ هذان الاختباران لا يتحققان من عرض اسم المتجر بصريًا. لم تُجرَ معاينة بالمتصفح. التالي: تجربة تغيير الاسم وحفظه من الإعدادات.

## إصلاح بقاء عملة الواجهة دولارًا بعد الحفظ (2026-09-13)

- السبب المثبت بالكود: الهيدر يقرأ الإعدادات مرة واحدة ويتجاهل الافتراضي عند وجود display_currency محفوظ؛ حفظ الإعدادات لا يُبلغ الواجهة بتغيرها.
- أضيف إشعار SettingsService.updated$ بعد نجاح الحفظ وربط الهيدر به. CurrencyService يطبق الافتراضي عند تغيره ويحتفظ باختيار الهيدر عندما يبقى الافتراضي نفسه، ويرجع للعملة الافتراضية المتاحة عند تعطيل العملة المختارة. أول تحميل بعد الإصلاح يصالح اختيار المتصفح القديم مع افتراضي الباك.
- نجح frontend/scripts/test-currency.cjs باستخدام المكونات والخدمات الحالية مع HTTP وأسعار صرف وهمية: اختيار دولار قديم، تطبيق الافتراضي، تحويل SYP/TRY، حفظ ناجح وفاشل، اختيار يدوي وتعطيل عملة. لم يُختبر مزود الصرف الفعلي أو المتصفح ولم تتغير بيانات الباك. الخطوة التالية: معاينة حفظ العملة بالمتصفح والتحقق من أسعار API الحقيقية. نجح فحص Angular عبر ngc --noEmit.

## إصلاح قائمة الموردين (2026-09-13)

- رُبطت أزرار الإضافة والتصدير ومسح البحث بالترجمة العربية والإنجليزية، وحُوّلت ترجمات القالب إلى pipe.
- عمود الإجمالي والتصدير يعرضان purchases_total: مجموع total_amount لطلبات الشراء غير الملغاة، بما فيها المسودات؛ ليس إجمالي الاستلامات. الرصيد المستحق في نافذة الدفع بقي balance دون تعديل البيانات.
- أضيف تجميع واحد لقائمة الموردين مع حقل اختياري في Resource ونوع الواجهة. نجح فحص الموردين الستة بالقراءة فقط (خمسة بإجمالي صفر)، وترقيم الصفحات والبحث الفارغ؛ عينة الاستجابة المالية: balance=0.00 وpurchases_total=110.00. نجح ngc --noEmit وفحص مفاتيح العربية والإنجليزية وPint واختبارات PhaseFiveResourceTest الثلاثة (16 تحققًا). لم تُختبر الواجهة بصريًا أو جلسة HTTP موثقة. التالي معاينة الأزرار والإجمالي والتصدير من المتصفح.

## نظام العملات المتعددة — 2026-09-13
- يدعم USD وSYP وTRY مع أسعار مباشرة من FxFeed.io.
- العملة الأساسية في قاعدة البيانات تبقى USD. جميع الأسعار مخزنة بالدولار.
- واجهة المستخدم تعرض الأسعار بعملة المستخدم المحددة عبر أنبوب `appCurrency`.
- سعر الصرف عند البيع يُخزّن في جدول orders للمرجع المحاسبي.
- مفتاح API في .env فقط (FXFEED_API_KEY)، يُخزّن 4 ساعات.
- yposCurrency في الهيدر بجانب اسم المستخدم يمينًا.
- أزرار العملة في إعدادات الدفع مع زر تحديث يدوي.
- تم التنفيذ بالكامل: باك إند (ExchangeRateService, ExchangeRateController, migrations, CheckoutService) + فرونت (exchange-rate.service.ts, currency.service.ts, appCurrency pipe, header dropdown, settings currency controls, sales currency support).
- التحقق: نجح ngc --noEmit وpint --dirty. لم تُختبر الواجهة بصريًا في المتصفح.
- الخطوة التالية: فحص API لأسعار الصرف وتجربة التحويل في المتصفح.

### إصلاح تطبيق أنبوب appCurrency عبر جميع القوالب — 2026-09-13
- صُحّح تنسيق SYP في `currency.service.ts`: من `SYP` كرمز إلى `ل.س` كلاحق عربي مع فواصل آلاف وأرقام صحيحة.
- صُحّح تنسيق TRY في `currency.service.ts`: إلى `₺` كسابقة مع تنسيق ت alphabet تركي.
- استُبدل كل `{{ '$' }}{{ value }}` بـ `{{ value | appCurrency }}` في القوالب:
  - `pos-dashboard.component.html`: صفوف الفواتير الأخيرة والمنتجات الأعلى مبيعًا (سطرين).
  - `pos-customers.component.ts`: إجمالي الفواتير ورصيد العميل وقائمة الفواتير المستحقة (3 أماكن).
  - `pos-suppliers.component.ts`: إجمالي المشتريات ورصيد المورد ومبلغ الدفعات (3 أماكن).
  - `pos-order-history.component.ts`: إجمالي الطلب وسعر الوحدة والإجمالي والفرعي والضريبة والمدفوع والمستحق (8 أماكن).
  - `pos-purchases.component.ts`: تكلفة الوحدة والإجمالي الفرعي وtotal_amount وتكلفة الاستلام (4 أماكن).
  - `pos-reports.component.ts`: صافي المبيعات والربح الخام ومتوسط القيمة ومبيعات الاتجاه والمنتجات الأعلى (6 أماكن).
  - `pos-products.component.ts`: سعر المنتج في الجدول (1 مكان).
  - `pos-sales.component.html`: رصيد العميل في قائمة الاختيار (1 مكان).
- أُضيف `ChangeDetectorRef` و`CurrencyService` مع اشتراك `currentCurrency$` لاستدعاء `detectChanges()` في 9 مكونات: Dashboard, Sales, Products, Customers, Suppliers, OrderHistory, Purchases, Reports, Inventory.
- التحقق: نجح `ngc --noEmit` بدون أخطاء. لم تُختبر الواجهة بصريًا في المتصفح.

### إصلاح قائمة العملات في الإعدادات العامة — 2026-09-13
- صُحّحت قائمة العملات في تبويب General: استُبدلت القائمة القديمة (USD, EUR, SAR, AED, EGP) بالقائمة الجديدة (USD, SYP, TRY) مع حقل `default_display_currency`.
- صُحّح تسمية الحقل إلى "العملة الافتراضية للعرض" / "Default Display Currency".
- أُضيف 3 إعدادات جديدة في `SettingSeeder`: `currency_syp_enabled`, `currency_try_enabled`, `default_display_currency`.
- صُحّح قائمة العملات في الهيدر: تُعرض العملات المفعلة فقط (USD دائمًا + SYP إذا مفعل + TRY إذا مفعل).
- تُحمَّل القيمة الافتراضية من إعدادات `default_display_currency`.
- أُضيفت ترجمات جديدة: `CURRENCY_SYP`, `CURRENCY_TRY`, `DEFAULT_DISPLAY_CURRENCY`, `DEFAULT_DISPLAY_CURRENCY_HINT`.
- التحقق: نجح ngc --noEmit وpint --dirty وseed للإعدادات. لم تُختبر الواجهة بصريًا.

## متابعة حذف المنتج (2026-09-10)

- المستخدم أفاد برسالة «تعذر اتمام العملية». سجل الباك أظهر رفض محاولات حذف حديثة من CatalogManagementService بسبب مخزون/حركات/روابط تشغيلية؛ لم يُحدد أي ارتباط بعينه. المسار DELETE والخدمة مرتبطان بشكل صحيح؛ الباك يرفض هذه الحالة بـ409.
- صُححت مفاتيح الترجمة غير الموجودة لرسائل حذف المنتج، وأضيف عرض مستمر لسبب الرفض بجانب المنتج وحالة «جارٍ الحذف». لم تُزل قيود سلامة البيانات ولم تُحذف منتجات للاختبار.
- اكتُشف خلل في auth.interceptor.ts: استخدام throwError(() => error) مع RxJS 6 يمرر دالة بدل الخطأ الأصلي. صُحح إلى throwError(error) ليصل سبب الباك إلى OperationFeedbackService. هذا الإصلاح يشمل أخطاء طلبات الواجهة عمومًا.
- التحقق: نجح اختبار معالج الواجهة بإصدار RxJS المثبت لحالات 409/403/500/401 مع الحفاظ على كائن الخطأ وتحويل تسجيل الدخول فقط لـ401؛ نجح تجميع SCSS. نجح فحص Angular النهائي ngc --noEmit. لم يُنفذ حذف فعلي ولم تُجرَ معاينة متصفح. التالي: تجربة المستخدم لعرض سبب رفض الحذف الواضح؛ المنتج ذو الارتباطات لا يزال محميًا من الحذف.

## آخر تعديل مطلوب — وضوح نموذج المنتج (2026-09-10)

- تصحيح لاحق بناءً على صورة المستخدم: كانت عناوين المستودع والكمية الافتتاحية وحد إعادة الطلب ومعرّفاتها قد نُسخت خطأً كـ«النوع» بسبب قراءة شرط ngIf بدل ngModel في التعديل السابق. صُححت العناوين والمعرّفات وأضيف شرح الكمية وحد إعادة الطلب؛ هما حقول رقمية فعلًا، والمستودع قائمة اختيار. فحص الترجمة السابق لم يكشف خطأ ربط العنوان؛ نجح فحص ربط عناوين الحقول الـ13 بـngModel وعدم تكرار المعرفات، ونجح تجميع Angular ngc --noEmit بعد التصحيح. لم تُجرَ معاينة متصفح.

- أضيفت عناوين ثابتة مرتبطة بجميع حقول إضافة/تعديل المنتج، مع شرح رمز SKU والتكلفة وسعر البيع والكمية الافتتاحية وحد إعادة الطلب بالعربية والإنجليزية. امتد الوصف بعرض النموذج، وأزيل سهم القائمة المكرر مع إبقاء السهم الأصلي للمتصفح. شروط ظهور حقول المخزون والتكلفة بقيت كما هي.
- التحقق: نجح فحص Angular ngc --noEmit وتجميع SCSS وفحص ترجمات النموذج بالعربية والإنجليزية (13 عنوان حقل)؛ لم تُختبر الواجهة بصريًا. لا تغييرات في حفظ البيانات أو الباك.
- التالي: فتح إضافة وتعديل المنتج والتأكد من وضوح العناوين مع قيم صفرية وقوائم مختارة، وعلى شاشة ضيقة.

## آخر تعديل مطلوب — صورة لوحة التحكم (2026-09-10)

- متابعة تعليق الواجهة أثناء التصوير: حُصر نسخ html2canvas بفرع المحتوى المطلوب وملفات head بدل نسخ عناصر الموقع غير المرتبطة، وأزيل ضبط scrollTop/scrollLeft داخل حلقة تعديل الأنماط لأنه يفرض إعادة حساب التخطيط لكل عنصر. أضيفت فرصة رسم حالة التصدير قبل بدء النسخ، مع إبقاء دقة 2× والمحتوى الكامل. التحقق الحالي: نجح فحص Angular ngc --noEmit؛ قياس الأداء والتجربة البصرية في المتصفح لم يُنفذا، ولا يمكن ضمان غياب أي توقف لأن الالتقاط ما زال على مسار الواجهة الرئيسي.

- عُدّلت `frontend/src/app/shared/screenshot.util.ts` لعزل نسخة الالتقاط عن حاويات Nebular القابلة للتمرير، وإظهار المحتوى الداخلي والنصوص المختصرة وقياس الأبعاد بعد التوسيع، وحفظ PNG بدقة 2× مع انتظار الخطوط. هذه الأداة مشتركة مع التقارير، فيشملها التعديل أيضًا.
- أزيل تعديل ارتفاع/overflow الصفحة الحية أثناء تصدير لوحة التحكم؛ التوسيع يتم على النسخة فقط.
- التحقق: نجح فحص Angular عبر `ngc -p src/tsconfig.app.json --noEmit`. لم تُختبر الصورة بصريًا في المتصفح. لا تغيير في الباك أو البيانات.
- التالي: تجربة زر صورة لوحة التحكم من أعلى الصفحة وبعد التمرير لآخرها، والتأكد من ظهور جميع البطاقات وصفوف الجدول وأسماء المنتجات بوضوح؛ وتجربة صورة التقارير لأنها تستخدم الأداة نفسها.

آخر تحديث: 2026-09-11. تكامل ngx-translate مكتمل ومُجمّع بنجاح: تثبيت @ngx-translate/core@14 + http-loader@7، ملفات i18n (ar.json, en.json) بنقاط 758 مطابقة. LanguageService تقرأ الإعدادات وتضبط اللغة وعناصر html dir/lang. ترجمة جميع ~500 نصًا عربي ثابت عبر ~20 مكون + تطبيق كامل مع `translate.instant()` و `| translate` pipe. تبديل اللغة من صفحة الإعدادات يُفعّل فوراً عبر PagesComponent على onLangChange. النموذج يعيد بناء القائمة عند تغيير اللغة.

## ابدأ من هنا

1. اقرأ هذا الملف أولًا، ثم تعليمات AGENTS.md والمهارات والقواعد المنطبقة على الملفات التي ستعدلها.
2. حدّد طلب المستخدم الحالي ونقطة التوقف أدناه. لا تبدأ اكتشاف المشروع من الصفر.
3. افتح الملفات المرتبطة بالمهمة فقط، وابحث باستخدام rg. افحص الحالة الحالية قبل تعديل كود قد يكون المستخدم غيّره.
4. بعد كل إضافة أو تعديل، حدّث الأقسام المعنية هنا: ما تغير، ما اختُبر، ما لم يُختبر، والخطوة التالية.
5. لا تحفظ هنا كلمات مرور أو Bearer tokens أو محتويات .env أو بيانات شخصية للعملاء.
6. عند تعارض هذا الملخص مع الكود، تحقّق من الكود المحدد وصحّح الملخص. تعليمات المستخدم الأحدث والتعليمات الأعلى أولوية مقدمة عليه.

## طريقة التعامل مع صاحب المشروع

- التواصل بالعربية الواضحة والقريبة من لهجته، بإجابات مختصرة تغطي كل أسئلته.
- وضّح الفرق بين: تم التنفيذ، نجح التجميع، نجح API، اختُبرت الواجهة يدويًا، اكتملت المرحلة.
- لا تقل إن المرحلة خالصة لمجرد إكمال دفعة منها، ولا تعِد نفس الفحوص الناجحة بلا سبب.
- المستخدم حريص جدًا على التوكنز؛ استخدم مراجعة محددة، تجنب قراءة الملفات الكبيرة كاملة بلا حاجة، ولا تعِد كتابة الموجود الصحيح.
- الأصل إعطاؤه خطوات اختبار قصيرة مع النتائج المتوقعة ليختبر بنفسه. عندما يطلب أن نختبر، نفحص الجزء المطلوب فقط.
- طلب المستخدم أن يكون فحص التكامل على قاعدة النظام الحالية، لا قاعدة منفصلة. هذا ليس إذنًا بإتلاف بياناته.
- ممنوع تشغيل migrate:fresh أو RefreshDatabase أو إعادة تهيئة/تفريغ قاعدة النظام. لا تشغّل مجموعة اختبارات على بياناته قبل التأكد من سلوكها.
- الفحوص الكتابية المصرح بها تكون على سجلات تجريبية واضحة، مع إزالة ما أنشأناه فقط والإبلاغ عن الأثر المتبقي في التدقيق/الحذف المنطقي. لا تغيّر الفواتير أو الأرصدة الفعلية.
- لا تغيّر كلمة مرور الأدمن أو صلاحياته للفحص. لا تلغِ توكن المستخدم دون توضيح؛ إلغاؤه سابقًا أدى إلى 401 في Swagger.
- الوصول للملفات لا يعني وجود جلسة دخول متصفح. التوكنات السابقة قد انتهت، ولا تولّد لنفسك صلاحية بديلة لتجاوز تسجيل الدخول.
- تقدير التوكنز تقريبي، ولا يمثل نسبة مؤكدة من حد الاشتراك.

## المشروع والتشغيل

- مشغّل التطوير المحلي الجديد: من جذر المشروع نفّذ node start-pos.cjs. يفحص المنافذ ويبدأ PHP وAngular كعمليات خلفية منفصلة، ولا يغيّر .env أو قاعدة البيانات. يتجاوز مشكلة npm.ps1 باستخدام Node مباشرة؛ لا يغيّر سياسة PowerShell.
- عند تشغيل Angular من عزل الأداة ظهر EPERM في watchpack أثناء readlink لمسار C:/Users/HP بعد انتهاء البناء. الحل التشغيلي هو طلب الموافقة لتشغيل المشغّل خارج العزل، لا تعديل node_modules أو صلاحيات النظام. التشغيل من طرفية المستخدم خارج العزل هو المسار المعتاد.
- سجلات المشغّل وملفات PID في مجلد pos-dev-<hash> داخل مجلد Windows المؤقت، ويطبع مسارها. لا يغلق أي عملية موجودة. إذا كان PID السابق حيًا دون منفذ، راجع السجل قبل إعادة المحاولة. بدء العملية لا يعني انتهاء بناء Angular، وإعادة تشغيل Windows تتطلب تشغيل المشغّل مجددًا.

- الجذر: C:/Users/HP/Desktop/pos_system
- Backend: backend/ — Laravel 13، PHP، MySQL، Sanctum Bearer، Swagger/OpenAPI.
- Frontend: frontend/ — Angular 15، ngx-admin/Nebular، واجهة عربية RTL.
- إصدارات الحزم الدقيقة تؤخذ من lock/الحزم المثبتة عند الحاجة؛ لا تفترض أن أرقام هذا المرجع ثابتة.
- توجد واجهات Blade قديمة في الباك؛ الواجهة المقصودة للتطوير اليومي هي Angular، ولا تستبدلها بـBlade.
- العنوان المحلي المعتاد للباك: http://127.0.0.1:8000
- مسارات API تبدأ مباشرة بـ /v1، وليس /api/v1.
- الفرونت المعتاد: http://127.0.0.1:4200
- Swagger: /docs/api. التوثيق الخام عبر /docs وفق إعداداته.
- إعداد اتصال Angular: frontend/src/environments/environment.ts؛ يسمح بتجاوز العنوان عبر window.__POS_CONFIG__.
- تشغيل الباك من backend: php artisan serve (إذا لم يكن شغالًا).
- تشغيل الفرونت من frontend: npm start (لا تشغّل نسخة ثانية إذا كان شغالًا).
- أحدث استئناف في 2026-09-07: كان الباك والفرونت متوقفين؛ شُغّل Laravel على 127.0.0.1:8000. افحص حالته مجددًا في الجلسة القادمة، فالعملية ليست ضمانًا لاستمرار التشغيل.
- توجد تعديلات كثيرة سابقة في backend، وبعض الملفات غير متتبعة. احفظ عمل المستخدم؛ لا reset/checkout أو تنظيف شامل.

## مصادر التعليمات والخطط

- backend/AGENTS.md: تعليمات Laravel المحلية.
- backend/.ai/rules/index.md: يحدد القواعد التي يلزم قراءتها حسب الملفات المعدلة.
- backend/.agents/skills/laravel-best-practices/SKILL.md: مهارة Laravel المحلية مع مراجع متخصصة.
- backend/codex-backend-full-plan.html: الخطة الموسعة، فيها سجل قديم وحديث؛ قد تتضمن تعليمات اختبار تاريخية تجاوزتها رسائل المستخدم.
- frontend/codex-pos-plan.html: خطة سابقة للواجهة؛ ارجع إليها فقط للجزء ذي الصلة.
- هذا الملف لا يلغي قراءة تعليمات AGENTS/المهارات الإلزامية، لكنه يختصر إعادة اكتشاف التطبيق.

## قواعد المجال التي لا تُكسر

- العميل Customer والمورد Supplier كيانان منفصلان؛ لا تعِد نموذج contacts مشترك.
- المنتج كتالوج؛ الكميات وحد إعادة الطلب والتكلفة المتوسطة لكل مستودع في inventory_balances.
- تغييرات المخزون عبر InventoryService، بمعاملات وقفل صفوف، وتسجل stock_movements غير القابلة للتعديل.
- أمر الشراء لا يزيد المخزون؛ الاستلام عبر GoodsReceiptService هو الذي يزيده ويربط دين المورد وحركة المخزون.
- البيع والمرتجعات عبر CheckoutService وSalesReturnService؛ لا تعدّل مبالغ الفواتير أو المخزون مباشرة من controller.
- الأرصدة والولاء مبنية على سجلات مالية append-only؛ استخدم خدمات الحسابات والولاء، لا تعدل الرصيد مباشرة.
- الورديات والصندوق عبر CashSessionService؛ الحركات النقدية تحتاج سببًا وسجلًا ثابتًا.
- كل إنشاء/تعديل/حذف ناجح له AuditLogService. القيم الحساسة محجوبة؛ لا تسجل الأسرار.
- تدقيق تفاصيل السجل عند العرض الصريح؛ لا تسجّل كل عملية list أو استرجاع model.
- تحقق المدخلات داخل Form Requests، والاستجابات عبر Resources، والتصريح عبر middleware والخدمات.
- Admin يتجاوز صلاحيات الأدوار، لكن ليس قواعد سلامة البيانات مثل حماية آخر أدمن.
- لا تسمح لمستخدم محدود بترقية نفسه أو إدارة صلاحيات أعلى منه.

## خريطة الملفات: افتح ما تحتاج فقط

| المجال | Backend | Angular |
| --- | --- | --- |
| المسارات والدخول | routes/api.php، AuthController، HasRolesAndPermissions، middleware | services/ وتهيئة auth المرتبطة بالمهمة |
| العملاء والموردون | CustomerController، SupplierController، PartyManagementService، PartyIndexRequest | pos-customers.component.ts، pos-suppliers.component.ts، customer.service.ts، supplier.service.ts |
| المنتجات والتصنيفات | ProductController، CategoryController، CatalogManagementService، CategoryTreeService، ProductImageService | pos-products.component.ts، product.service.ts، category.service.ts |
| المستخدمون والأدوار | UserManagementService، RoleManagementService، AccessManagementGuard | شاشاتها وخدماتها الموجودة؛ ابحث بالاسم |
| التدقيق | AuditLogController، AuditLogIndexRequest، AuditLogResource، AuditLogService | لم ننشئ شاشة Angular لسجل التدقيق في العمل الموثق هنا |
| أسماء الصلاحيات بالعربي | config/access_labels.php، RoleResource، PermissionResource | تحتاج الواجهة استخدام name_ar وdescription_ar لعرضهما |
| المخزون والجرد | InventoryController، InventoryCountController، InventoryService | ابحث عن inventory |
| المشتريات | PurchaseOrderService، GoodsReceiptService، SupplierAccountService، SupplierProductService | purchase-order.service.ts وشاشات المشتريات |
| الفواتير وحساب العميل | SaleController، OrderIndexRequest، OrderResource، CustomerAccountService | order.service.ts، pos-customers.component.ts |
| التوثيق | app/OpenApi.php، config/l5-swagger.php | Swagger من الباك |
| التصدير | — | src/app/shared/export.util.ts |

المسارات المختصرة أعلاه داخل backend/app/Http/Controllers/Api أو Requests أو Resources أو app/Services بحسب النوع؛ ملفات Angular داخل frontend/src/app/pages أو services.
لبعض الشاشات ملفات SCSS منفصلة حاليًا؛ لا تعِد الأنماط القديمة inline فوق تعديلات المستخدم.

## الخطة وحالتها الفعلية

| المرحلة | النطاق | الحالة المرجعية |
| --- | --- | --- |
| 0 | Swagger/OpenAPI | موصوفة كمكتملة سابقًا؛ حدث توثيق المسارات عند تغيرها |
| 1 | المخزون والجرد | توجد خدمات ومسارات وتعديلات واسعة؛ لا تدّعِ اعتماد كل تفاصيلها من هذا الملخص |
| 2 | المشتريات والاستلام وحساب المورد | موجودة، مع فحوص سابقة لأجزاء منها؛ يلزم الرجوع للجزء المطلوب فقط |
| 3 | الفواتير والمرتجعات | موجودة؛ الطباعة وBlade مؤجلان في الخطة الأصلية |
| 4 | التدقيق والصلاحيات | تعديلات الباك منجزة وفحوص محددة ناجحة؛ لا توجد شاشة تدقيق Angular جديدة |
| 5 | استكمال الإدارة الأساسية | المرحلة الحالية، قيد التنفيذ؛ ليست مكتملة بجميع بنودها |
| 6 | البيانات المرجعية | تالية: المستودعات والوحدات والضرائب والصناديق وطرق الدفع |
| 7 | الفلاتر المحفوظة | مطلوبة لاحقًا |
| 8 | الاستيراد | Excel/CSV؛ قراءة صورة فاتورة المورد بالذكاء الاصطناعي مؤجلة بطلب المستخدم |
| 9 | الجودة والتكامل المحاسبي | مراجعة مالية وصلاحيات وتزامن واختبارات ثم تحديد المحاسبة الخارجية |

المقصود بصورة الفاتورة هو فاتورة المورد، وليس فاتورة الزبون. لا تنفذ التكامل مع Gemini أو غيره الآن ولا تضف مفاتيح API.
طلبات تاريخية أخرى: تصدير Excel حقيقي .xlsx، وتصدير صورة بشكل الشاشة مع تنسيقها. توجد أدوات/حزم تصدير، لكن هذا المرجع لا يثبت اجتياز فحصهما البصري.

## المنفذ في المرحلة 4

- GET /v1/audit-logs وGET /v1/audit-logs/{auditLog} بصلاحية audit_logs.view، مع تجاوز Admin.
- فلاتر user_id، action، entity_type، entity_id، from، to، page، per_page بين 1 و100.
- name_ar وdescription_ar للأدوار والصلاحيات، مع بقاء name/key البرمجيين.
- حماية آخر أدمن ومنع منح/إدارة صلاحيات أعلى، وتصحيح القيم القديمة/الجديدة عند تغيير الدور.
- حجب الأسرار عند تسجيل التدقيق وعند عرض سجلات قديمة، دون إعادة كتابة السجلات القديمة.
- حذف أمر شراء يحتاج purchases.delete، وليس purchases.edit.
- 53 اختبارًا/826 تحققًا نجحت سابقًا على قاعدة منفصلة قبل إصرار المستخدم على قاعدة النظام؛ هذا سجل تاريخي وليس إذنًا بإعادة تلك الطريقة.
- فحص لاحق مباشر على قاعدة النظام نجح للأدمن: القوائم، إنشاء مستخدم وعميل تجريبيين، تعديل العميل، التدقيق، التعريب. الكاشير التجريبي مُنع من المستخدمين والتدقيق بـ403.

## المنفذ في المرحلة 5

- العملاء والموردون: الباك يحترم per_page بدل 25 ثابتة؛ الافتراضي 25، والحد 1–100.
- البحث search حتى 100 حرف بالاسم والشركة والهاتف والإيميل والرقم الضريبي، مع ترتيب ثابت وإبقاء الفلاتر بروابط الصفحات.
- ربط البحث ومسحه بواجهتي Angular؛ البحث الصريح بزر أو Enter، وليس طلبًا عند كل حرف.
- إضافة الرقم الضريبي للنموذجين، وتحسين الرجوع للصفحة السابقة عند حذف آخر سجل فيها.
- إضافة تعديل وحذف المنتجات في Angular والخدمة المقابلة، مع تحميل التفاصيل قبل التعديل.
- تعديل المنتج لا يرسل كمية افتتاحية مجددًا؛ نوع المنتج ثابت في نموذج التعديل الحالي، وتعديل المخزون له مساره الخاص.
- السعر/التكلفة لا يُرسلان عند عدم التغيير؛ التكلفة غير المعروضة للمستخدم لا تتحول إلى صفر عند حفظ تعديل آخر.
- إضافة المنتجات كانت موجودة أصلًا. إنشاء منتج خدمة اختُبر بالـAPI؛ إنشاء منتج مخزني بكمية افتتاحية واستخدام النموذج بالمتصفح لم يُختبرا ضمن هذه الدفعة.
- أضيف OrderIndexRequest: customer_id UUID، due_only boolean، page موجب، per_page بين 1 و100.
- GET /v1/orders يدعم تلك الفلاتر؛ due_only=1 يعني due_amount أكبر من صفر.
- سجل العميل واختيار الفواتير المستحقة يرسلان customer_id للباك بدل تصفية صفحة من كل الفواتير محليًا.
- توثيق فلاتر الفواتير عُدّل في app/OpenApi.php؛ قد يحتاج إعادة توليد Swagger لعرضه.

## سجل التحقق وحدوده

- 2026-09-08: نجح تجميع Angular عبر ngc --noEmit بعد إزالة التصفية المحلية لفواتير العميل. لم ينجح فحص API المصادق لأن التوكن الأخير أعاد 401؛ لا فحص بصري ولا تغييرات ببيانات النظام في هذا الاستئناف.

- المستخدم أكد نجاح صفحات العملاء والموردين.
- اختبار API البحث وحجم الصفحة ورفض القيم غير الصالحة للعملاء والموردين نجح على الباك الحالي وقت الفحص.
- اختُبر إنشاء وتعديل وحذف عميل ومورد ومنتج خدمة تجريبيين؛ نجح البحث بالشركة والرقم الضريبي وSKU، ورفض سعر سالب بـ422.
- أزيلت السجلات التجريبية من القوائم؛ الحذف المنطقي وسجل التدقيق يبقيان آثارًا متوقعة.
- تجميع Angular عبر ngc --noEmit نجح بعد تعديلات المنتجات والبحث، ثم بعد ربط فلاتر الفواتير.
- تنسيق PHP للملفات المعدلة نجح.
- فحص فلاتر الفواتير الجديدة عبر HTTP لم يتم: الباك لم يستجب في آخر محاولة. لا تخلط نتائج CRUD السابقة بهذا الفحص.
- لا يوجد إثبات فحص بصري شامل للواجهات المعدلة، ولا إثبات اكتمال المرحلة 5.
- 2026-09-07: تم إصلاح تخطيط واجهة المنتجات ونقطة البيع: شجرة التصنيفات بمؤشرات توسيع واضحة، أزرار تعديل/حذف أفقية بمسافة 8px، شريط بحث أفقي بـ flexbox، تحويل شبكة بطاقات المنتجات في نقطة البيع إلى جدول منظم بـ 6 أعمدة (#، الاسم، SKU، السعر، المخزون، الإجراء)، وبحث مدمج. تجميع Angular نجح بدون أخطاء.
- 2026-09-07: إصلاح شامل لواجهة المستخدم: توحيد ستايل جميع `<select>` و `nb-select` بألوان Ocean Blue (#0077B6 حدود، #023E8A نص، #00B4D8 hover) عبر `:host ::ng-deep` في كل صفحة. إصلاح شرائح البحث الأفقية في جميع الصفحات (Products, Sales, Suppliers, Customers, Purchases, Order History, Users, Roles, Reports, Inventory). إضافة رسم بياني شريطي (ECharts) للمبيعات الشهرية في صفحة التقارير. إضافة رسم بياني دائعي (Pie) لتوزيع المخزون حسب التصنيف + رسم بياني أفقي للمنتجات منخفضة المخزون في صفحة المخزون. استيراد NgxEchartsModule في PagesModule. تجميع Angular نجح بدون أخطاء.
- 2026-09-07: إصلاح ألوان الرسوم البيانية في التقارير والمخزون: استبدال ألوان Nebular Theme بألوان Ocean Blue المحددة (#03045E, #023E8A, #0077B6, #0096C7, #00B4D8, #48CAE4). إزالة البيانات الوهمية الثابتة من رسوم التقارير واستخدام بيانات حقيقية من ReportService. إصلاح رسوم المخزون الدائرية والأفقية بألوان Ocean Blue مع رسائل بديلة عند عدم وجود بيانات. إزالة اعتماد NbThemeService من كلا الملفين. تجميع Angular نجح بدون أخطاء.

## إصلاح المودالات ونموذج إضافة المنتج (2026-09-14)

- أُعيد كتابة SCSS لـ `DetailsDialogComponent`: أنيميشن دخول، هيدر gradient Ocean Blue، توكنز موحدة، دعم form-grid داخل المودال، responsive للشاشات الصغيرة.
- أُضيف backdropblur وfox-radius وأقوى ظل في `_components.scss` لـ nb-dialog.
- نموذج إضافة المنتج يفتح الآن كنافذة منبثقة عبر NbDialogService بدل nb-card inline، بنفس نمط التعديل.
- تحسين `.create-form` و`.product-field` و`.form-actions` داخل الـ dialog_products SCSS.
- التحقق: نجح `ngc --noEmit` بدون أخطاء. لم تُختبر الواجهة بصريًا في المتصفح.
- تحديث role.md: 2026-09-14.

## إصلاح أزرار Save المفقودة في مودالات الدفع والإضافة/التعديل (2026-09-14)

- السبب: قوالب `#editFormTemplate` و `#paymentFormTemplate` في الموردين والعملاء كانت تفتح كـ Dialog لكن لا تحتوي على أزرار Save أو Cancel. فوتر `DetailsDialogComponent` يعرض زر Close فقط.
- الحل: أُضيف `<div class="form-actions">` بزر Save و Cancel داخل القوالب الأربعة. أُضيف `showFooter: false` لجميع مكالمات `dialogService.open()` في الموردين والعملين وتاريخ الطلبيات والمشتريات. أُضيف تنسيقات `.form-actions` لملفات SCSS للموردين والعملين.
- الملفات: pos-suppliers.component.ts/scss، pos-customers.component.ts/scss، pos-order-history.component.ts، pos-purchases.component.ts.
- التحقق: نجح `ngc --noEmit` بدون أخطاء. لم تُختبر الواجهة بصريًا.
- تحديث role.md: 2026-09-14.

## نقطة الاستئناف التالية

### إصلاح UX نقطة البيع — 2026-09-13
- Issue 1: أزيل زر "إتمام الدفع" من الشريط العلوي، ونُقّل فقط لأسفل السلة مع `*ngIf="currentShift"` ليختفي عند عدم وجود شفت مفتوح.
- Issue 2: أُعيد تصميم نموذج فتح الشفت: عند اختيار صندوق عليه شفت مفتوح، يُظهر بطاقة "يوجد شفت مفتوح" مع معلومات من فتحه ومتى، وزر "متابعة الشفت الحالي" (للمالك أو من لديه صلاحية) و"إغلاق الشفت إداريًا" (للأدمن فقط). أُضيفت طريقة `continueShift()` في TS. أُضيفت حقول الإغلاق الإداري في نموذج الإغلاق بدلاً من نموذج الفتح.
- Issue 3: صُحّح تخطيط نموذج فتح الشفت: `<nb-card>` مع ترويسة Ocean Blue gradient (#023E8A → #0077B6)، حقل الصندوق بعرض كامل، النقد الافتتاحي والملاحظات في صف واحد 50%/50%، زر التأكيد يمينًا. أُزيلت الأقسام الفارغة والتخطيط المكسور.
- Issue 4 (Rolling Drawer): أُضيف GET `/v1/shifts/last-closed?register_id=xxx` في `CashSessionController::lastClosed()` مع route جديد بصلاحية `shifts.open`. يُرجع `closing_cash` و`closed_at` واسم من أغلق الشفت الأخير للصندوق. في الفرونت، أُضيف `ShiftService::lastClosed()` و`LastClosedShift` interface، وعند اختيار صندوق بدون شفت مفتوح يُحمَّل النقد الختامي للشفت السابق ويُملأ تلقائيًا في حقل النقد الافتتاحي مع تلميح أزرق.
- التحقق: نجح `pint --dirty` و`ngc --noEmit` و9 اختبارات CashSession بدون أخطاء. لم تُختبر الواجهة بصريًا في المتصفح.
- تحديث role.md: 2026-09-13.

### تعزيز إغلاق الوردية — 2026-09-13
- أُضيف عمود `closing_notes` (text, nullable) إلى جدول `shifts` عبر migration.
- صُحّح `CashSessionService::closeSession()`: عند فرق != 0 يجب إدخال `closing_notes` (422 إذا فارغ). عند فرق مطلق > 50 يجب إدخال `admin_override_reason` (422 إذا فارغ). يُسجَّل الفرق في AuditLog مع type overage/shortage.
- صُحّح `expectedCash()` من private إلى public.
- صُحّح `CloseShiftRequest` لقبول `closing_notes` و`admin_override_reason`. صُحّح `ForceCloseShiftRequest` لقبول `closing_notes`.
- صُحّح `ShiftResource` و`Shift` model لعرض وحفظ `closing_notes`.
- في الفرونت: أُضيف textarea لملاحظات الإغلاق في نموذج إغلاق الشفت، مع تمريرها عبر `ShiftService::close()`.
- تحديث `CashSessionApiTest` ليعدي بعد تمرير `closing_notes` عند وجود فرق.
- أُصلح باغ في `lastClosed()`: `name` → `full_name` (الجدول يستخدم `full_name`).
- التحقق: نجح `pint --dirty` و`ngc --noEmit` و9 اختبارات CashSession (28 تحققًا). فُحص API مباشرة: اختبار 1 (فرق≠0 بدون ملاحظات → 422 عربي)، اختبار 2 (فرق>50 بدون admin_override → 422 إنجليزي)، اختبار 3 (فرق صغير مع ملاحظات → 200 مع حفظ closing_notes)، اختبار 4 (فرق>50 مع admin_override → 200)، اختبار 5 (فرق صفر → 200 بدون ملاحظات)، اختبار 6 (force-close مع ملاحظات → 200)، اختبار 7 (last-closed endpoint → 200 معاسم المسؤول). أُنظّفت 4 شفاتات تجريبية من قاعدة البيانات.
- تحديث role.md: 2026-09-13.

### إنشاء StockMovementController — 2026-09-13
- أُنشئ `app/Http/Controllers/Api/StockMovementController.php` مع method `index()` يستقبل `StockMovementIndexRequest`.
- يُرجع قائمة paginated مع `data`، `current_page`، `last_page`، `per_page`، `total` (عادي) أو `data` فقط مع `?all=1` للتصدير.
- الفلاتر: `product_id`، `warehouse_id`، `user_id`، `movement_type`، `from`، `to`، `search` (اسم المنتج أو SKU)، `all`، `per_page` (افتراضي 25).
- Eager load: `product:id,product_name,sku`، `warehouse:id,name`، `user:id,full_name`. يرتيب حسب `occurred_at DESC`.
- أُضيف `all` boolean rule إلى `StockMovementIndexRequest`.
- المسار: `GET /v1/stock-movements` بصلاحية `inventory.view`.
- التحقق: نجح pint، المسار مسجل، API مباشر: اختبار1 (قائمة افتراضية 25)، اختبار2 (filtration=sale)، اختبار3 (search)، اختبار4 (date from)، اختبار5 (all=1 يُرجع 1214 سجل بدون pagination)، اختبار6 (eager loading product/warehouse/user)، اختبار7 (invalid type → 422).
- تحديث role.md: 2026-09-13.

- تسجيل الخروج والشفت — 2026-09-13
- فحص AuthController وCashSessionService أكد استدعاء autoCloseOnLogout لإغلاق الشفت المفتوح بواسطة المستخدم نفسه. لم يُعدّل الباك أو أي شفت حقيقي.
- أزيل مسح الجلسة والتحويل إلى الدخول من معالج فشل logout في HeaderComponent؛ يعرض خطأ API أو رسالة مترجمة ويتيح إعادة المحاولة. أضيف منع الطلبات المكررة أثناء الانتظار. يبقى مسح الجلسة بعد نجاح الطلب في AuthService. عند 401 يستمر AuthInterceptor بمعالجة انتهاء المصادقة، مع تنبيه أن إغلاق الشفت غير مؤكّد وضرورة الدخول للتحقق؛ لا إغلاق تلقائي مضمون بمجرد انتهاء التوكن.
- التحقق: node scripts/test-logout.cjs نجح لمحاكاة 7 حالات فشل، منع النقرات المكررة وإعادة المحاولة الناجحة؛ نجح ngc --noEmit. لا اختبار متصفح أو logout فعلي ولا سجلات تجريبية. سبب فشل طلب المستخدم الأصلي غير مثبت؛ الإصلاح يمنع إخفاء فشل الطلب. التالي تجربة خروج المستخدم من شفته والتحقق من حالته، أو فحص رسالة API الظاهرة إن فشل.

### إصلاح مصادقة Stripe — 2026-09-13
- السبب المثبت: قيم غير صالحة لمفاتيح Stripe في جدول settings كانت تطغى على الإعدادات الصحيحة بالخادم والواجهة. أضيف StripeKeyResolver لاختيار المفتاح السري الصحيح، مع إبقاء أولوية مفتاح الإعدادات الصحيح وعدم تبديله تلقائيًا عند رفض Stripe له. أضيف fallback مماثل للمفتاح العام في StripeService.
- منع حفظ مفاتيح بصيغة خاطئة في تحديث الإعداد المفرد والجماعي برسائل عربية، وتحسين رسالة المصادقة وعدم تسجيل نص استثناء المصادقة الذي قد يتضمن المفتاح. لم تُعدّل القيم المخزنة أو .env أو الصلاحيات؛ القيم القديمة غير الصالحة متروكة ويجري تجاوزها، وعند تعديل إعدادات الدفع يجب مسحها أو إدخال مفاتيح صحيحة.
- التحقق: طلب قراءة فقط GET إلى Stripe /v1/balance بالمفتاح المختار أعاد 200؛ تطابق المفتاح العام الافتراضي بالواجهة مع إعداد الخادم. PHPUnit المستهدف StripeKeyResolverTest: أربع اختبارات / 37 تحققًا دون قاعدة بيانات؛ اختبار اختيار مفتاح Angular بمحاكاة: أربع حالات ناجحة. نجح ngc --noEmit وPint للملفات المعنية وgit diff --check.
- الحدود والخطوة التالية: لم تُنفّذ دفعة أو تُختبر واجهة الدفع بصريًا، ولا توجد سجلات تجريبية جديدة. حدّث المتصفح وجرب تدفق الدفع؛ فحص المصادقة لا يثبت نجاح الدفع كاملًا. المرحلة 5 لم تُستكمل في هذه الجلسة؛ الطلب الحالي اقتصر على عطل Stripe.

### واجهة حركات المخزون (Frontend) — 2026-09-13
- أُنشئ `stock-movement.service.ts` مع `list()` (paginated) و`listAll()` (للتصدير) يستدعيان `GET /v1/stock-movements`.
- أُنشئ مكون `PosStockMovementsComponent` في مجلد `pos-stock-movements/` (TS + SCSS) بقالب inline:
  - فلاتر: بحث، نوع الحركة (9 أنواع)، المستودع، من/إلى تاريخ، مسح الفلاتر
  - جدول: التاريخ، المنتج (اسم + SKU)، النوع (badge ملوّن)، الكمية (+/- أخضر/أحمر)، المستودع، المستخدم، الملاحظات، المرجع
  - ترقيم صفحات (25 لكل صفحة)، حالة فارغة وحالة تحميل
  - زر تصدير Excel يستخدم `listAll()` + `downloadExcel()`
- أُضيف `MENU.STOCK_MOVEMENTS` بالعربي والإنجليزي.
- أُضيف المسار `GET /pages/stock-movements` مع `PermissionGuard` بصلاحية `inventory.view`.
- أُضيف `PosStockMovementsComponent` إلى `PagesModule` declarations.
- أُضيف 38 مفتاح ترجمة `STOCK_MOVEMENTS.*` إلى ar.json وen.json (772 مفتاح لكل ملف، متوازنان).
- التحقق: نجح `ngc --noEmit` و`pint --dirty` بدون أخطاء. لم تُختبر الواجهة بصريًا في المتصفح.
- تحديث role.md: 2026-09-13.

### إزالة stripe_secret_key من قاعدة البيانات والواجهة — 2026-09-13
- أُزيل `stripe_secret_key` بالكامل من قاعدة البيانات والباك إند والفرونت. المفتاح السري يُقرأ الآن من `.env` فقط عبر `config('stripe.secret')`.
- **باك إند**: `StripeController` يقرأ من config مباشرة. `BulkUpdateSettingsRequest` لا يتحقق من stripe_secret_key بعد الآن. `UpdateSettingRequest` لا يتعامل مع stripe_secret_key. `SettingSeeder` لا يزرع stripe_secret_key. Migration جديد يحذف الصف من جدول settings. `StripeKeyResolverTest` محدّث.
- **فرونت**: حُذف حقل stripe_secret_key من النموذج والcontrol group والdefaults. أُضيف `autocomplete="off"` على حقل publishable key. أُضيف مربع معلومة `stripe-secret-notice` بأيقونة قفل يوضح أن المفتاح السري مخزّن على الخادم فقط.
- **ترجمات**: أُضيف `STRIPE_SECRET_INFO` بالعربي والإنجليزي. حُذفت `STRIPE_SECRET_KEY` و`STRIPE_SECRET_HINT`. صُححت مفاتيح `STRIPE_HINT` و`STRIPE_PUB_KEY` و`STRIPE_PUB_HINT` لتتوافق مع القالب.
- التحقق: نجح pint وngc و4 اختبارات StripeKeyResolver (25 تحققًا). فُحص API مباشرة: (1) stripe_secret_key لم يعد موجودًا في الإعدادات، (2) تغيير اللغة فقط → 200، (3) إرسال stripe_secret_key في الطلب → يُتجاهل بهدوء (200، لا يُكتب في DB)، (4) StripeController يقرأ المفتاح من config/`.env`.
- Migration: `2026_09_13_200124_remove_stripe_secret_key_from_settings`
- تحديث role.md: 2026-09-13.
- المشكلة: حفظ الإعدادات يفشل عند تغيير اللغة فقط لأن `stripe_secret_key` المخزنة `ChangeMe!123` تفشل التحقق من regex وتمنع حفظ كل الحقول.
- الحل: أُضيف `prepareForValidation()` إلى `BulkUpdateSettingsRequest` يقارن قيم Stripe الواردة بالمخزنة. إذا لم تتغير، تُحذف من الطلب قبل التحقق.
- التحقق: 4 اختبارات API مباشرة: (1) تغيير اللغة فقط → 200 ✅، (2) Stripe invalid → 422 ✅، (3) Stripe valid → 200 ✅، (4) إعادة إرسال Stripe كما هي → 200 ✅. نجح pint.
- تحديث role.md: 2026-09-13.

- ngx-translate integration مكتمل ومُجمّع — جميع النصوص العربية قابلة للترجمة فوراً. يمكن تبديل اللغة من صفحة الإعدادات وستظهر كل الشاشات بالعربية أو الإنجليزية.
- تم تعطيل ميزة تغيير كلمة المرور من الواجهة.
- تم إصلاح لوحة التحكم: يجب أن يكون `must_change_password = false` لكي تعمل جميع endpoints البيانات. تم تعديل `AdminSeeder` ليضبط القيمة `false` عند الإنشاء الأولي.

### Stripe test mode integration (2026-09-10)
- **Status**: Code complete; pending real Stripe test keys for live testing.
- What was done:
  - Backend: installed `stripe/stripe-php`, added placeholder `STRIPE_KEY`/`STRIPE_SECRET` to `.env`, created `config/stripe.php`, created `StripeController::createPaymentIntent()` at `POST /v1/payment/create-intent`.
  - Frontend: installed `@stripe/stripe-js`, created `StripeService` with `createPaymentIntent()` and `getStripe()`, modified POS sales to show all payment methods (removed cash-only filter), added Stripe Elements modal for card payments.
  - Modified `shift.service.ts` checkout() to accept optional `referenceNumber`.
  - Added `stripeKey` to `environment.ts` (placeholder needs real publishable key).
- What was NOT tested:
  - The endpoint with real Stripe keys (needs user to provide test keys from Stripe dashboard).
  - End-to-end card payment flow in browser.
- Next steps:
  1. User provides real Stripe test keys (publishable pk_test_... and secret sk_test_...) from Stripe dashboard.
  2. Update `frontend/src/environments/environment.ts` with real publishable key.
  3. Update `backend/.env` with real secret key.
  4. Test the flow: create a card payment → open Stripe modal → enter test card (4242 4242 4242 4242) → confirm → verify invoice created.

استئناف 2026-09-10: توقّف الخدمة كان مصحوبًا بعدم وجود مستمع على 8000 أو4200. أضيف start-pos.cjs وشُغلت الخدمتان، ونجح /up و/v1/settings/public وdocs/api بـ200. حُدّث Swagger المولد وتأكد وجود statement ومنتجات المورد. لا تغييرات ببيانات أو صلاحيات أو .env، ولا استخدام للتوكن القديم.

2026-09-11: ngx-translate integration مكتمل ومُجمّع. ثُبّت @ngx-translate/core@14 + @ngx-translate/http-loader@7 (بـ --legacy-peer-deps بسبب @angular/google-maps). أُنشئ ar.json وen.json بنقاط 758 متطابقة. تمت ترجمة جميع ~500 نص عربي ثابت في ~20 مكون Angular مع `translate.instant()` و `| translate` pipe. أُضيف TranslateModule.forRoot() إلى AppModule مع HttpLoaderFactory. LanguageService تقرأ الإعدادات وتضبط اللغة. PagesComponent يجدد القائمة عند onLangChange. Header يستخدم `action: 'logout'` بدل النص العربي للتوثيق. build نجح بـ npm run build بدون أخطاء.

توثيق نتائج متبقية من جلسة 2026-09-08: نجح فحص قراءة statement لثلاثة عملاء (السجل فارغ لديهم) ومنتجات ثلاثة موردين مع مطابقة supplier_id وعدم تكرار product_id. نجح اختبارا PhaseFiveResourceTest بـ11 تحققًا دون اتصال بقاعدة البيانات، وتجميع Angular بعد إضافة الشاشتين نجح. ما زال يلزم فحص قيود الحذف الجديدة على التجريبي فقط، والحالات المالية غير الفارغة، وفحص عرض الكشف والمنتجات الجديدة داخل المتصفح. فحص CRUD السابق بالمتصفح لا يثبت اختبار هاتين الإضافتين.

تقدم 2026-09-08: اختُبرت Angular عبر Chrome منفصل: إضافة/تعديل/حذف عميل ومورد برقم ضريبي ومنتج خدمة، وظهور رفض السعر السالب. أُنشئ منتج مخزني تجريبي SKU=CODEX-UI-STOCK-P5-20260908-A، id=01a0826c-f5aa-700f-9303-49e038eaf664، بكمية 2 وتكلفة صفر؛ بقي دون حذف بسبب حركة opening الواحدة. بقيت الكمية 2 بعد تعديل الاسم. السجلات التجريبية الأخرى حُذفت منطقيًا وبقي التدقيق. أضيف كود كشف حساب العميل ومنتجات المورد وقيود حذف المرتبط؛ فحص هذه الإضافات الجديدة لم يكتمل بعد. الخطوة التالية التحقق من endpoints والتجميع والتوثيق وفحص الرسائل، دون حذف سجلات حقيقية.

استئناف لاحق في 2026-09-08: قُبل التوكن الجديد. نجح فحص GET /v1/orders: per_page=5، customer_id (أعاد فاتورة تخص العميل فقط)، due_only=1 ودمجه مع العميل (كلاهما أعاد قائمة فارغة؛ لم تثبت حالة دين موجبة). القيم page=0 وper_page=101 وcustomer_id=bad وdue_only=bad أعادت 422. التوثيق المولد /docs يحتوي الفلاتر الأربعة، فلا يحتاج إعادة توليد. لم تتغير فواتير أو بيانات. التالي اختبار Angular؛ كان المنفذ 4200 متوقفًا وبدأ تشغيله.

استئناف 2026-09-08: الباك استجاب، لكن GET /v1/orders بالتوكن الأخير الذي قدمه المستخدم أعاد 401؛ لم يُلغَ التوكن أو يُنشأ وصول بديل. لا تعتبر فلاتر الفواتير مختبرة. أزيلت response.data.filter من قائمتي فواتير العميل والمستحق في pos-customers.component.ts، مع الحفاظ على إرسال customer_id وdue_only للباك. لا تغيير ببيانات النظام ولا سجلات تجريبية جديدة. المطلوب للمتابعة بالترتيب: توكن من تسجيل دخول جديد، ثم الفحص المباشر؛ لم تُختبر الواجهة بصريًا.

آخر تحديث: 2026-09-10. تم إضافة تكامل Stripe test mode: باك إند (StripeController, route, composer deps) + فرونت (StripeService, Stripe modal, تعديل payment methods filter). الكود مكتمل بانتظار مفاتيح Stripe الحقيقية للاختبار.

1. التحقق من تشغيل الباك، والحصول على توكن صالح فقط عند الحاجة لفحص API؛ لا تعِد استخدام أسرار قديمة من المحادثة دون تحقق.
2. فحص GET /v1/orders مع customer_id وdue_only=1 وper_page، ورفض القيم غير الصالحة، دون إنشاء فواتير حقيقية.
3. تجربة سجل العميل والفواتير المستحقة من الواجهة، ثم نموذج المنتج خصوصًا المنتج المخزني.
4. استكمال مراجعة بنود المرحلة 5 الموسعة: كشف حساب العميل ودفعاته، ربط منتجات المورد إن كان بالنموذج، والقيود المرجعية عند الحذف؛ تحقق من الموجود قبل البناء.
5. لا تنتقل للمرحلة 6 بوصف المرحلة 5 مكتملة قبل حصر هذه البنود وتنفيذ الناقص والتحقق منه.

## أوامر مرجعية عند الحاجة

هذه ليست أوامر تُشغّل تلقائيًا عند قراءة الملف.

- بحث موجه: rg -n "الكلمة" مسار/محدد
- حالة تعديلات الباك: git -C backend status --short
- فحص تجميع Angular من frontend:
  node node_modules/@angular/compiler-cli/bundles/src/bin/ngc.js -p src/tsconfig.app.json --noEmit
- تنسيق PHP من backend: php vendor/bin/pint --dirty --format agent ثم مسارات الملفات المحددة؛ لا تنسق تعديلات المستخدم غير المرتبطة.
- تحديث Swagger من backend: php artisan l5-swagger:generate --no-interaction
- عند No parameters في Swagger: افحص الملف المولد والتوثيق المستخدم ثم أعد التوليد/التحديث؛ لا تفترض تلقائيًا أن المشكلة مجرد cache.
- 401 يعني تسجيل الدخول/التوكن غير صالح أو مفقود؛ 403 منع صلاحية أو قيد وصول، و422 رفض مدخلات. لا تساوِ بينها.

## كيفية تحديث هذا المرجع

- حدّث المعلومات الحالية في مكانها، ولا تلصق كل الحوار أو المخرجات أو فروق الكود.
- أضف سطرًا مختصرًا للتغيير وتاريخ آخر تحديث، وحدث نقطة الاستئناف.
- لكل فحص: حدّد النطاق والنتيجة والبيئة، ولا تكتب «كل شيء ناجح» لفحص محدود.
- اختصر التاريخ القديم عندما لا يعود مؤثرًا، واحتفظ بالقرارات والقيود والأخطاء التي تمنع تكرار العمل.
- عند تغيير المرحلة، حدّث هذا الملف وخطة الباك الموسعة لتفادي تناقض نقطتي المتابعة.

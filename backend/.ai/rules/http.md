---
paths:
  - 'app/Http/**'
---

# Http

## Validate and serialize at the HTTP boundary
Use dedicated Form Request classes for validation and API Resource classes for JSON responses. Enforce authorization through route middleware and permissions.
